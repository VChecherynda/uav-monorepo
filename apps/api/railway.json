{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "npm install && npm run build --workspace=@uav/shared && npm run build --workspace=apps/api"
  },
  "deploy": {
    "startCommand": "node apps/api/dist/server.js"
  }
}
